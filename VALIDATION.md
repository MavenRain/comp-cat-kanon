# Validation

Validated on 2026-09-10 against the revisions recorded in the source manifest and
toolchain lock.

| Check | Result |
| --- | --- |
| Original OCaml `dune build @all @runtest`, isolated archive | 320 tests across 25 suites passed |
| Pinned Kanon compiler with constructor patch | Built from an isolated source archive |
| Compiler kernel, WebAssembly, and surface test executables | Passed |
| Parameterized constructor acceptance and Wasm value | Passed |
| Constructor with a wrong field type | Rejected with a type mismatch |
| Global source names | No duplicates across 39 modules |
| Native Kanon behavioral tests | 108 passed |
| OCaml differential observations | 324 matched |
| Fixture regeneration in a fresh archive | Byte-identical to committed observations |
| README build and Wasm execution example | Prints `4` |

The 324 observations comprise 64 finite-set/function-space cases, 120 graded
truth-value cases, and 140 temporal verdict/depth cases. Reference observations
are in `port/oracle.jsonl`, generated by `port/oracle.ml`. They are additional
checks, not a translation of all 320 source tests.

Behavioral tests cover universal mediators, factorization, a deliberately wrong
uniqueness candidate, commuting squares, path cycles, category-of-elements
membership, unification clash and occurs checks, shared-base theory colimits,
temporal proof-rule rejection, guarded recursion, memo misses/hits, malformed
arrow compatibility, and explicit source limitations.

The source compiler's `dune @runtest` alias has working-directory assumptions for
two suites. Its test executables were run from the compiler root. The original
compiler and OCaml source checkouts were left unchanged.

Run `npm test` to repeat the port checks. The bootstrap and oracle commands in
`README.md` reproduce the compiler prerequisite and reference observations.
